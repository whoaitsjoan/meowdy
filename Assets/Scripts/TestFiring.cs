using UnityEngine;

public class TestFiring : MonoBehaviour
{
 /*   [SerializeField] private Transform meowProjectile;
    private void Awake()
    {
        GetComponent<MeowAim>().MeowFire += TestFiring_MeowFire;
    }

    private void TestFiring_MeowFire(object sender, MeowAim.MeowFireEventArgs e)
    {
        Instantiate(meowProjectile, e.pointerEndPosition, Quaternion.identity);
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
